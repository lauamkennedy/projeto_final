/* ======== CONFIG GERAL ======== */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: "Poppins", sans-serif;
}

body {
    background: linear-gradient(135deg, #0d0d0d, #1a1a1a, #000000);
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
}

/* ======== CONTAINER ======== */
.container {
    width: 100%;
    max-width: 420px;
    background: rgba(255, 255, 255, 0.06);
    border-radius: 18px;
    padding: 35px;
    backdrop-filter: blur(12px);
    box-shadow: 0 0 25px rgba(0, 255, 255, 0.25),
                0 0 8px rgba(0, 255, 255, 0.5) inset;
    animation: fadeIn 0.6s ease;
}

/* Animação ao abrir */
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(-10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* ======== TÍTULO ======== */
.container h1 {
    text-align: center;
    color: #00f0ff;
    margin-bottom: 25px;
    font-size: 28px;
    text-shadow: 0 0 10px #00eaff;
}

/* ======== INPUTS ======== */
input[type="text"],
input[type="number"] {
    width: 100%;
    margin: 10px 0;
    padding: 13px;
    border-radius: 10px;
    border: 1px solid #00eaff;
    background: rgba(0, 0, 0, 0.5);
    color: white;
    outline: none;
    font-size: 16px;
    transition: 0.3s;
    box-shadow: 0 0 8px rgba(0, 255, 255, 0.25);
}

input[type="text"]:focus,
input[type="number"]:focus {
    border-color: #0ff;
    box-shadow: 0 0 12px #00eaff, 0 0 20px #00eaff inset;
    transform: scale(1.03);
}

/* ======== BOTÃO ======== */
input[type="submit"] {
    width: 100%;
    padding: 14px;
    margin-top: 10px;
    font-size: 17px;
    font-weight: bold;
    border-radius: 10px;
    border: none;
    color: #000;
    background: #00eaff;
    cursor: pointer;
    transition: 0.3s;
    box-shadow: 0 0 15px #00eaff;
}

input[type="submit"]:hover {
    background: #00c2cc;
    box-shadow: 0 0 20px #00eaff, 0 0 30px #00eaff;
    transform: translateY(-3px);
}

/* ======== MENSAGEM ======== */
.mensagem {
    text-align: center;
    margin-top: 15px;
    font-size: 18px;
}
